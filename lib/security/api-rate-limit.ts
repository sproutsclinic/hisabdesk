ï»¿/**
 * =========================================================
 * API Rate Limit Guard (Enterprise Protection)
 * HisabDesk ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã¢â‚¬Å“ Abuse & Cost Control Layer
 * =========================================================
 *
 * PURPOSE
 * Protect APIs from:
 *
 *   ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Â¦ÃƒÂ¢Ã¢â€šÂ¬Ã…â€œÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã¢â‚¬Å“ brute force attacks
 *   ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Â¦ÃƒÂ¢Ã¢â€šÂ¬Ã…â€œÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã¢â‚¬Å“ webhook spam
 *   ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Â¦ÃƒÂ¢Ã¢â€šÂ¬Ã…â€œÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã¢â‚¬Å“ scraping
 *   ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Â¦ÃƒÂ¢Ã¢â€šÂ¬Ã…â€œÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã¢â‚¬Å“ accidental infinite loops
 *   ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã¢â‚¬Â¦ÃƒÂ¢Ã¢â€šÂ¬Ã…â€œÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã¢â‚¬Å“ cost explosion (Supabase usage)
 *
 * WHY THIS FILE EXISTS
 * ---------------------------------------------------------
 * middleware.ts ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬ÃƒÂ¢Ã¢â‚¬Å¾Ã‚Â¢ global limit (coarse)
 * THIS FILE     ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬ÃƒÂ¢Ã¢â‚¬Å¾Ã‚Â¢ per-route precise limit (fine-grained)
 *
 * Example:
 *   login ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬ÃƒÂ¢Ã¢â‚¬Å¾Ã‚Â¢ 10/min
 *   webhooks ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬ÃƒÂ¢Ã¢â‚¬Å¾Ã‚Â¢ 300/min
 *   exports ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬ÃƒÂ¢Ã¢â‚¬Å¾Ã‚Â¢ 5/min
 *
 * =========================================================
 *
 * ========================
 * USAGE (inside API route)
 * ========================
 *
 * import { rateLimitGuard } from "@/lib/security/api-rate-limit"
 *
 * await rateLimitGuard(req, {
 *   key: "login",
 *   limit: 10,
 *   windowMs: 60_000
 * })
 *
 * =========================================================
 */

"use server"

import { NextResponse } from "next/server"

/* =========================================================
   MEMORY STORE (Edge safe)
========================================================= */

type Bucket = {
  count: number
  ts: number
}

const store = new Map<string, Bucket>()

/* =========================================================
   HELPERS
========================================================= */

function getIP(req: Request) {
  return (
    req.headers.get("x-forwarded-for") ||
    req.headers.get("x-real-ip") ||
    "unknown"
  )
}

/* =========================================================
   MAIN GUARD
========================================================= */

type Options = {
  key: string // route name (login/export/webhook)
  limit: number // max requests
  windowMs: number // time window
}

export async function rateLimitGuard(
  req: Request,
  options: Options
) {
  const ip = getIP(req)

  const now = Date.now()

  const bucketKey = `${options.key}:${ip}`

  const bucket = store.get(bucketKey)

  /* ------------------------------------------------------
     NEW WINDOW
  ------------------------------------------------------ */

  if (!bucket || now - bucket.ts > options.windowMs) {
    store.set(bucketKey, {
      count: 1,
      ts: now,
    })
    return
  }

  /* ------------------------------------------------------
     LIMIT EXCEEDED
  ------------------------------------------------------ */

  if (bucket.count >= options.limit) {
    throw NextResponse.json(
      { error: "Too many requests" },
      { status: 429 }
    )
  }

  /* ------------------------------------------------------
     INCREMENT
  ------------------------------------------------------ */

  bucket.count++
}

/* =========================================================
   WRAPPER (cleaner usage)
========================================================= */

export function withRateLimit(
  handler: (req: Request, ctx: any) => Promise<Response>,
  options: Options
) {
  return async (req: Request, ctx: any) => {
    await rateLimitGuard(req, options)
    return handler(req, ctx)
  }
}
